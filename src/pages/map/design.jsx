export const styles = theme => ({
  root: {
    position: 'fixed',
    width: '100vw',
    minHeight: '100vh',
    backgroundColor: theme.palette.primary.dark,
    zIndex: 2,
    top: 0,
    left: 0,
  }
});